- [go] 2026-09-17 小心：后端原始 SQL 必须 postgres/sqlite 两种方言都能跑——时间一律由 Go 侧传参，禁用 `NOW()`/`INTERVAL`/`WITH ... UPDATE ... RETURNING`；新增模型或字段时必须同步 `internal/database/sqlite.go` 的 SQLite DDL。危害：本机没装 PostgreSQL，本地走 `database.driver=sqlite`，方言不兼容要等启动或调用到那个接口时才报语法错误 / "no such column"，而 `go build`、`go vet` 全绿看不出来。@backend-go/internal/repository, backend-go/internal/database
- [go] 2026-09-17 小心：凡涉及权限/可见性的查询，超管判定必须与 RBAC 的“全放行”对齐——复用 `isSuperuser(c)`（handler）或 `c.Get("is_superuser")`（middleware），不要在角色/菜单关联上另立一套。危害：种子超管没有任何角色，`/accounts/users/me/` 菜单为空，出现“接口全能调、侧边栏却是空的”割裂。@backend-go/internal/service/menu_service.go, backend-go/internal/handler
- [全栈] 2026-09-17 小心：前端路由是静态写在 `adminx-ui/src/router/index.ts` 的，菜单表只决定侧边栏可见项——种子菜单的 `path` / `permission_code` 必须与前端 route 及 `meta.permission` 逐条对齐，否则菜单能看见但点进去 404（无此路由）或被守卫拦住（权限码不匹配，超管因全放行不暴露）。危害：非超管用户“看得见用不了”，排查时容易只往后端 RBAC 上找。@backend-go/cmd/init-data, adminx-ui/src/router/index.ts
- [go] 2026-09-17 小心：GORM 的列名推导会拆缩写（`DiskReadMBps` → `disk_read_m_bps`，不是 `_mbps`），手写 SQLite DDL 时必须按 GORM 的 DBName 写，别按字段名/JSON 名猜。危害：列名不一致时写入报 `table xxx has no column named ...`，而 build/vet/编译都看不出来（`internal/database` 的守卫测试会拦）。@backend-go/internal/database/sqlite.go
